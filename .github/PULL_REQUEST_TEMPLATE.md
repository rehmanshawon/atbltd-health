# Pull Request

## Summary

- Resolves: #issue_id
- Feature / Fix description:

## Verification

- [ ] Unit tests pass (`npm test`)
- [ ] Backend coverage threshold met (`npm run test:cov:backend`)
- [ ] Zero-network tests pass (`npm run test:isolated`)
- [ ] Lint and typecheck pass (`npm run lint && npm run typecheck`)
- [ ] No files over 500 LOC introduced
